# Open Meteo CLI

Open-Meteo offers free weather forecast APIs for open-source developers and non-commercial use. No API key is required.

Learn more at [Open Meteo](https://open-meteo.com).

## Install

### Go

```
go install github.com/mvanhorn/printing-press-library/library/other/open-meteo/cmd/open-meteo-pp-cli@latest
```

### Binary

Download from [Releases](https://github.com/mvanhorn/printing-press-library/releases).

## Quick Start

### 1. Install

See [Install](#install) above.

### 2. Set Up Credentials

Get your API key from your API provider's developer portal. The key typically looks like a long alphanumeric string.

```bash
export OPEN_METEO_APIS_API_KEY="<paste-your-key>"
```

You can also persist this in your config file at `~/.config/open-meteo-apis-pp-cli/config.toml`.

### 3. Verify Setup

```bash
open-meteo-pp-cli doctor
```

This checks your configuration and credentials.

### 4. Try Your First Command

```bash
open-meteo-pp-cli forecast
```

## Usage

Run `open-meteo-pp-cli --help` for the full command reference and flag list.

## Commands

### forecast

Manage forecast

- **`open-meteo-pp-cli forecast list`** - 7 day weather forecast for coordinates


## Output Formats

```bash
# Human-readable table (default in terminal, JSON when piped)
open-meteo-pp-cli forecast

# JSON for scripting and agents
open-meteo-pp-cli forecast --json

# Filter to specific fields
open-meteo-pp-cli forecast --json --select id,name,status

# Dry run — show the request without sending
open-meteo-pp-cli forecast --dry-run

# Agent mode — JSON + compact + no prompts in one flag
open-meteo-pp-cli forecast --agent
```

## Agent Usage

This CLI is designed for AI agent consumption:

- **Non-interactive** - never prompts, every input is a flag
- **Pipeable** - `--json` output to stdout, errors to stderr
- **Filterable** - `--select id,name` returns only fields you need
- **Previewable** - `--dry-run` shows the request without sending
- **Read-only by default** - this CLI does not create, update, delete, publish, send, or mutate remote resources
- **Offline-friendly** - sync/search commands can use the local SQLite store when available
- **Agent-safe by default** - no colors or formatting unless `--human-friendly` is set

Exit codes: `0` success, `2` usage error, `3` not found, `4` auth error, `5` API error, `7` rate limited, `10` config error.

## Use as MCP Server

This CLI ships a companion MCP server for use with Claude Desktop, Cursor, and other MCP-compatible tools.

### Claude Code

```bash
claude mcp add open-meteo open-meteo-pp-mcp -e OPEN_METEO_APIS_API_KEY=<your-key>
```

### Claude Desktop

Add to your Claude Desktop config (`~/Library/Application Support/Claude/claude_desktop_config.json`):

```json
{
  "mcpServers": {
    "open-meteo": {
      "command": "open-meteo-pp-mcp",
      "env": {
        "OPEN_METEO_APIS_API_KEY": "<your-key>"
      }
    }
  }
}
```

## Health Check

```bash
open-meteo-pp-cli doctor
```

Verifies configuration, credentials, and connectivity to the API.

## Configuration

Config file: `~/.config/open-meteo-apis-pp-cli/config.toml`

Environment variables:
- `OPEN_METEO_APIS_API_KEY`

## Troubleshooting
**Authentication errors (exit code 4)**
- Run `open-meteo-pp-cli doctor` to check credentials
- Verify the environment variable is set: `echo $OPEN_METEO_APIS_API_KEY`
**Not found errors (exit code 3)**
- Check the resource ID is correct
- Run the `list` command to see available items

---

Generated by [CLI Printing Press](https://github.com/mvanhorn/cli-printing-press)
