// Copyright 2026 trevin-chow. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newOptionsPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagDate int

	cmd := &cobra.Command{
		Use:   "options <symbol>",
		Short: "Options chain for a symbol (calls and puts)",
		Long:  "Shortcut for 'options get'. Options chain for a symbol (calls and puts)",
		Example: `  # Nearest-expiry options chain for Apple
  yahoo-finance-pp-cli options AAPL

  # Specific expiration date (Unix timestamp)
  yahoo-finance-pp-cli options TSLA --date 1735689600 --json

  # For filtering by moneyness/DTE, use the richer transcendence command:
  yahoo-finance-pp-cli options-chain AAPL --moneyness otm --max-dte 45`,
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v7/finance/options/{symbol}"
			if len(args) < 1 {
				return usageErr(fmt.Errorf("symbol is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "symbol"))
			}
			path = replacePathParam(path, "symbol", args[0])
			params := map[string]string{}
			if flagDate != 0 {
				params["date"] = fmt.Sprintf("%v", flagDate)
			}
			data, prov, err := resolveRead(c, flags, "options", false, path, params)
			if err != nil {
				return classifyAPIError(err)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.compact {
					filtered = compactFields(filtered)
				}
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagDate, "date", 0, "Expiration unix epoch (omit for nearest expiration)")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
